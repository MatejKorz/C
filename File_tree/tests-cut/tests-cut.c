#define CUT
#include "cut.h"


/*
 *   ┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
 *   ┃   𝐓𝐡𝐢𝐬 𝐢𝐬 𝐭𝐡𝐞 𝐟𝐢𝐥𝐞 𝐲𝐨𝐮 𝐚𝐫𝐞 𝐥𝐨𝐨𝐤𝐢𝐧𝐠 𝐟𝐨𝐫   ┃
 *   ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
 *
 * Here you can add your own unit tests if you wish.
 * There are «NOT» sanity tests (“testy nanečisto”); for those you need to
 * look into ‹tests-basic.sh›.
 */

/* TIP: Including header files
 *
 * Tests for this homework are compiled as if they were able to see header
 * files directly. So, to include ‹utils.h› in tests, you can use two
 * forms:
 *
 * 	#include <utils.h>
 * 	#include <../utils.h>
 *
 * Choose any of these, but be consistent. */


TEST(dummy) {
	/* This is just a dummy test. If you wish to write your own Unit tests,
	 * remove this nonsense. */
	CHECK(1 != 2);
}
